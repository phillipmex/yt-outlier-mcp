# YouTube Data API v3 key (BYO-key model — each user supplies their own).
# Create at https://console.cloud.google.com/apis/credentials with the
# "YouTube Data API v3" enabled. Free quota: 10,000 units/day.
YOUTUBE_API_KEY=
